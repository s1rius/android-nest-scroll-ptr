import wtf.s1rius.ptr.buildsrc.*

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath ClassPaths.kotlinPlugin
        classpath ClassPaths.gradlePlugin
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}